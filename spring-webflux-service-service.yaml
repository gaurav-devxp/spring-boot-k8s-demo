apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring-webflux-service
  name: spring-webflux-service
spec:
  ports:
  - name: 8080-8080
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: spring-webflux-service
  type: ClusterIP
status:
  loadBalancer: {}
